name: Minify HTML

on:
  push:
    branches: [ develop, beta, production ]
    paths:
      - 'index.html'
      - 'scripts/**'
      - 'package.json'
      - '.github/workflows/**'
  pull_request:
    branches: [ develop, beta, production ]
    paths:
      - 'index.html'
      - 'scripts/**'
      - 'package.json'
      - '.github/workflows/**'
  # Ermöglicht manuelles Auslösen des Workflows
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual build and test'

# Explizite Permissions für den Workflow
permissions:
  contents: write

jobs:
  minify:
    runs-on: ubuntu-latest
    
    # Verhindert parallele Ausführung des Workflows
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1  # Shallow clone für schnelleren Checkout

    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: Install dependencies
      run: |
        bun install --frozen-lockfile --no-progress

    - name: Minify HTML
      run: bun run build

    - name: Commit minified files
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add dist/
        if ! git diff --quiet --staged; then
          git commit -m "chore: update minified HTML [skip ci]"
          git push origin HEAD:${{ github.ref_name }}
        fi 